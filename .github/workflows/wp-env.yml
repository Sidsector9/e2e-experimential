name: Install Easy Engine
on:
  push:
    branches:
      - smoke-testing
  pull_request:
    types: [labeled]
jobs:
  run-e2e:
    if: ${{ github.event.label.name == 'e2e-ready' }}
    runs-on: ubuntu-18.04
    steps:
      - run: echo "Hello"

